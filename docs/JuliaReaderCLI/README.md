# Julia Reader CLI

Documentation for the command-line harness lives alongside the code:

- **Terminal bundle** (optional `julia reader` launcher): [`terminal/README.md`](../../terminal/README.md)
- **Repo-wide setup**: [`SETUP.md`](../../SETUP.md) and [`README.md`](../../README.md) (Install / Usage)
- **Entry points**: `julia-reader` or `python -m julia_reader` — see [`pyproject.toml`](../../pyproject.toml) `[project.scripts]`

Add deeper CLI docs here as this folder grows.
